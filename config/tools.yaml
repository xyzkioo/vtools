# 根目录 run_tools.py 的默认说明配置。
# 桌面工作台使用的默认配置映射。
tool: diagnostics
diagnostics_config: model_diagnostics/config/config.yaml
speed_config: speed_test/benchmark_config.yaml
visualization_config: model_visualization/config/config.yaml
compression_config: model_compression/config/config.yaml
