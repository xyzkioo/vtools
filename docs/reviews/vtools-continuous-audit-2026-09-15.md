# 持续修复与验收

用户目标：持续修复并复查，直到检查中不再发现问题。本轮静态检查、入口检查和可在当前环境执行的回归检查均未再发现未修复问题。

## 当前已修改

- 阶段追踪接口分别接收逻辑 image_id 与产物文件名，单图 canonical、阶段明细和汇总使用同一逻辑标识。
- UI 复用诊断、测速和压缩的模块解析器；缺省 modules 使用后端默认规则，显式空集合保持关闭。压缩 operation 规则移入公共解析器，避免 UI 和入口解释不同。
- 修正阶段追踪 class_scores 对已解码概率重复 sigmoid 的问题。
- 新增真实 Qt 控件测试与实际 trace_stage 文件写出测试（小型检测头替身、真实 PyTorch 张量）。

## 当前验证证据

发现 `/home/xyzkioo/miniconda3/envs/yolo/bin/python` 可导入 PyTorch 和 PySide6。其 pytest 因缺少 iniconfig 启动失败，改用 unittest 运行现有测试；此前“缺 torch”仅限系统解释器。

- 压缩 10 项通过，含真实张量蒸馏损失与剪枝测试。
- 测速 43 项：42 通过、1 跳过。
- 诊断 5 项通过。
- 新增及此前回归 7 项通过，含真实 Qt 切页/保存/路径更换，及实际阶段导出文件的 ID 和概率校验。
- 语法编译通过；git diff --check 通过。

这些结果不证明全仓无问题。仍需继续检查原审计清单、默认入口组合、真实模型路径及文档一致性；完整 GPU/TensorRT/K230 业务验收尚未完成。

## 后续入口组合检查

- UI 加载压缩配置时同步模型任务，避免分类配置被默认 --task detect 覆盖；显式 classify 优先于残留的检测数据路径推断。
- 用户取消当前测速类型的所有模块时阻止启动，避免缺少 --only 导致后端恢复默认；TensorRT 页面不再强制给 checkpoint-only 加入导出和构建。
- 检测头 top-k 筛选失败时报告 unsupported，不生成虚假的空最终预测；最终目标 CAM 在追踪不成功时不使用未经验证的候选索引。
- 真实 Qt/阶段导出及其他新增回归现共 11 项通过，压缩现共 12 项通过。此前测速 43 项（1 跳过）和诊断 5 项结果仍有效。新修改的语法编译与差异空白检查通过。
- 尚需持续复核原始审计项与运行组合，目标保持进行中。

## 注册表与改名事务检查

- 注册表拒绝陈旧实例覆盖其他实例新建的同名分支，也拒绝重新写入其他实例已删除的记录。新增同名创建冲突、不同分支增量合并测试；运行记录中的 id 不能覆盖 add_run 的明确参数。
- COCO 备份名称增加唯一标识，避免同秒内备份相互覆盖；写新 JSON 前保留原文件，临时文件完成后原子替换，失败时清理临时文件。
- 改名回滚先检查所有来源，再复用支持交换与故障恢复的两阶段改名，避免缺少后续文件时把前面的文件留在临时位置。
- 新增文件交换/回滚、缺失文件回滚预检、连续备份和写出失败保护测试。当前 tests 15 项通过，压缩 15 项通过；语法与差异检查通过。目标继续进行中。

## 图片与视频输出保护

- 图片统一扩展名转换先检查目标重复与已有文件冲突，避免 overwrite 模式下同 stem 的不同图片覆盖彼此。
- 图片编码与视频帧写入使用同目录临时文件，成功后才替换旧输出；失败时保留原文件并清理临时文件。
- 修正透明调色板图片转 JPEG 时未使用透明通道合成白底的问题。
- 新增重复目标预检、图片/视频编码失败保护与调色板透明度测试。当前 tests 共 19 项通过，语法编译与差异检查通过。仍需完成其余原始审计项与整体文档验收。

## 统一入口、转换与 UI 状态文件

- 非法或无法解析的 tools.yaml 现在返回明确错误，不再静默回退执行诊断；仓库聚合配置中的子配置路径仍按仓库根解析。
- K230 转换与校验将可选运行依赖延迟到实际功能调用，缺包环境也能查看帮助；增加尺寸、颜色和阈值校验，拒绝空输出张量，校准图片稳定排序，并原子发布 Kmodel。
- UI 历史文件拒绝非列表旧内容并采用临时文件原子替换，写入失败保留原记录。
- 当前 tests 29 项通过；压缩 21 项、诊断 5 项通过；测速 43 项通过（1 项按环境跳过）。全仓语法编译与 git diff --check 通过。目标继续进行中。

## 可视化与输入边界复查

- 可视化图片先完整编码到同目录临时文件，再原子替换目标；编码失败保留已有产物。离线报告对图片名和链接进行 HTML 转义。
- 输入尺寸必须为正数，检测置信度与 IoU 必须是 0 到 1 的有限值，最大检测数必须大于 0；未知特征通道模式和聚合方式会明确报错。
- 视频解帧的单文件入口会检查扩展名，不再把文本等任意文件交给视频解码器。
- 最新验证：通用回归 38 项、压缩 21 项、测速 44 项（1 项按环境跳过）、诊断 6 项通过；16 个命令行入口的帮助启动、全部 YAML/JSON 解析、受版本控制 Markdown 的本地链接、全仓语法编译和差异格式检查均通过。
- 当前环境没有可执行的真实 GPU TensorRT 和 K230 板端端到端条件；相关路径已完成无硬件回归与错误路径检查，不把缺少硬件实测描述成代码缺陷。
