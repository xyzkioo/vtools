# vtools 第二轮复检修复记录

日期：2026-09-15

本轮处理第二次复检列出的 9 项：统一一键测速成功状态；修正新分支顺序压缩判断；保留 YAML 高级页后续编辑；保持 ONNX 声明输入 dtype；在 checkpoint CSV 中保留检查状态；让 UI 模块按钮基于完整 YAML 集合更新；让 legacy models 服从 `predict.generate`；统一单图/目录数据集图片 ID，并更新与实际行为冲突的文档。

隔离复现已验证：一致性 `passed` 返回 0；起始 head 为 None 的同轮压缩使用当前 head；FP16 归一化输入为 float16；PyTorch/TensorRT 两份 checkpoint CSV 均保留 failed/error；YAML 中后编辑的 auto 不再被旧图形值覆盖；单图、目录和同 stem 不同扩展名在可视化与诊断中得到一致 ID。

不依赖 PyTorch 的现有测试为 54 passed。三个子项目全量测试结果为 56 passed、2 failed；两项失败均在导入 torch 时终止，因为 `/usr/bin/python3` 未安装 PyTorch，并非断言失败。compileall 和 git diff --check 通过。当前环境未完成真实 PyTorch 训练、Qt 窗口、GPU/TensorRT 或 K230 端到端验收。
