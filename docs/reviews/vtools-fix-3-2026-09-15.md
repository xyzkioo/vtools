# 第三轮修复记录

四项复检问题已修改：图片 ID 始终保留扩展名，不再根据输入集合追加后缀；可视化提供 input.dataset_root/--dataset-root 对齐自定义布局；配置编辑器离开一页时提交数据、进入另一页时重建显示；手动修改配置路径立即刷新完整模块集合。

图片 ID 协议有兼容性变化：旧版不带扩展名或带 #jpg 的预测文件需要重新生成。自定义目录布局必须提供与诊断一致的数据集根目录，未提供时明确报错。使用说明已更新至 model_visualization/README.md。

验证：4 项新隔离回归和 54 项既有测试通过，共 58 passed；语法编译和 git diff --check 通过。新回归覆盖子集/目录/自定义布局标识、跨页按顺序提交、配置模块重新加载。UI 测试抽取实际状态方法使用控件替身，不等同真实 Qt 信号与窗口验收。当前解释器缺少 torch，之前全量测试中的两项依赖测试本轮未重跑；未执行训练、GPU、TensorRT 或 K230 端到端流程。
